% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_tolerance.R
\name{plot_tolerance}
\alias{plot_tolerance}
\title{Plot the tolerance scoring curve for a species and variable}
\usage{
plot_tolerance(
  species,
  variable,
  season = NULL,
  n_steps = 500L,
  show_exclusion = TRUE,
  title = NULL,
  colour = "#2166ac"
)
}
\arguments{
\item{species}{Character string. Species key (e.g. \code{"ostrea_edulis"}).
Accepts the same formats as \code{\link[=predict_oyster]{predict_oyster()}}.}

\item{variable}{Character string. Name of the scored variable to plot
(e.g. \code{"temperature"}, \code{"salinity"}, \code{"ph"}, \code{"omega_aragonite"}).
Use \code{get_species_tolerances(species)$scored} to see available variables.}

\item{season}{Character or \code{NULL}. For seasonal variables (temperature),
specifies which season's parameters to display. One of \code{"winter"},
\code{"spring"}, \code{"summer"}, \code{"autumn"}. If \code{NULL} (default), the base
parameters are used; pass \code{"all"} to overlay all four seasons.}

\item{n_steps}{Integer. Number of points on the x-axis (default \code{500}).
More = smoother curve.}

\item{show_exclusion}{Logical. If \code{TRUE} (default), overlays the hard
exclusion threshold(s) from \code{tolerances$exclusions} as vertical red lines.}

\item{title}{Character or \code{NULL}. Plot title. \code{NULL} = auto-generated.}

\item{colour}{Character. Curve colour (default \code{"#2166ac"}, a marine blue).}
}
\value{
A \code{ggplot2} object (invisibly). Printed as a side effect. Can be
further customised with standard \code{ggplot2} layers. Falls back to base R
\code{plot()} if \code{ggplot2} is not installed.
}
\description{
Draws the suitability scoring function for any scored environmental variable
directly from the species tolerance specification -- no survey dataset is
required. The curve shows exactly what score the model assigns to each value
of the variable: 1 (optimal), declining gradients through acceptable and
poor zones, and 0 at hard limits.

For a data-driven response curve derived from an actual survey (partial
dependence), use \code{\link[=sensitivity_analysis]{sensitivity_analysis()}} instead.
}
\examples{
# Temperature curve for O. edulis (base / year-round parameters)
plot_tolerance("ostrea_edulis", "temperature")

# Summer temperature only
plot_tolerance("ostrea_edulis", "temperature", season = "summer")

# All four seasons overlaid
plot_tolerance("ostrea_edulis", "temperature", season = "all")

# pH curve for the European flat oyster
plot_tolerance("ostrea_edulis", "ph")

# Aragonite saturation for Pacific oyster
plot_tolerance("magallana_gigas", "omega_aragonite")

# Save to a temporary file
p <- plot_tolerance("ostrea_edulis", "salinity")
ggplot2::ggsave(file.path(tempdir(), "salinity_tolerance.png"), p, width = 8, height = 5)
}
