% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/score_variables.R
\name{score_locations}
\alias{score_locations}
\title{Score all locations in a dataframe}
\usage{
score_locations(df, tolerances, verbose = FALSE)
}
\arguments{
\item{df}{A dataframe processed by \code{\link[=check_exclusions]{check_exclusions()}}.}

\item{tolerances}{Species tolerance list from \code{\link[=get_species_tolerances]{get_species_tolerances()}}.}

\item{verbose}{Logical. Print per-variable scoring summary (default \code{FALSE}).}
}
\value{
The input dataframe with additional columns:
\itemize{
\item \code{suitability}: weighted score [0, 1]
\item \code{suitability_class}: "High" / "Moderate" / "Low" / "Very Low" / "Excluded"
\item \code{limiting_factors}: comma-separated names of the variables most limiting
the score (NA if all variables score >= 0.65)
\item \verb{score_<variable>}: one column per scored variable present in the data
}
}
\description{
Applies \code{\link[=.score_row]{.score_row()}} across every non-excluded row. Excluded rows
receive \code{suitability = 0}. Adds per-variable score columns and a
\code{limiting_factors} column identifying the variables most constraining
the score at each location.
}
