% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/wave_exposure.R
\name{score_wave_exposure}
\alias{score_wave_exposure}
\title{Score wave exposure from fetch or measured wave height}
\usage{
score_wave_exposure(
  result,
  fetch_col = NULL,
  wave_height_col = NULL,
  fetch_km = NULL,
  wave_height_m = NULL,
  wind_speed_ms = 12,
  depth_limit = TRUE,
  verbose = TRUE
)
}
\arguments{
\item{result}{Dataframe from \code{\link[=predict_oyster]{predict_oyster()}}. Must contain \code{lat}, \code{lon}.
Optional columns used if present: \code{depth_m}, \code{fetch_km}, \code{wave_height_m}.}

\item{fetch_col}{Character. Name of a column in \code{result} containing fetch
values in km. Alternative to supplying a scalar \code{fetch_km}.}

\item{wave_height_col}{Character. Name of a column in \code{result} containing
measured/modelled Hs in metres.}

\item{fetch_km}{Numeric scalar. Uniform fetch in km applied to all rows
(overridden by \code{fetch_col} if both supplied).}

\item{wave_height_m}{Numeric scalar. Uniform Hs in metres applied to all
rows (overridden by \code{wave_height_col} if both supplied).}

\item{wind_speed_ms}{Numeric. Design wind speed in m/s used for JONSWAP
calculation (default 12 m/s = moderate gale, Beaufort 6, typical design
condition for coastal aquaculture siting).}

\item{depth_limit}{Logical. Apply depth-limited wave breaking cap
(Hs_max = 0.6 * depth_m). Default TRUE.}

\item{verbose}{Logical. Default TRUE.}
}
\value{
\code{result} with additional columns:
\itemize{
\item \code{wave_hs_m}: significant wave height (m) \u2014 computed or supplied
\item \code{wave_exposure_score} \link{0,1}: 0 = fully sheltered, 1 = severely exposed
\item \code{wave_exposure_class}: "Sheltered" / "Moderate" / "Exposed" / "Severe"
\item \code{wave_exposure_note}: gear implication flag
\item \code{wave_source}: method used ("measured", "jonswap_fetch", "depth_proxy")
}
}
\description{
Produces a wave exposure score \link{0,1} and significant wave height estimate
for each survey location. High wave exposure reduces gear feasibility and
can destabilise restoration reef structures and spat bags.

\strong{Input options:}
\itemize{
\item \code{fetch_km}: effective fetch in kilometres (distance to nearest land or
obstruction in the prevailing wind direction). Most useful for estuarine,
sea loch, or enclosed bay sites. Can be measured from GIS (e.g. in QGIS
using the Fetch tool) or estimated from chart inspection.
\item \code{wave_height_m}: directly measured or modelled significant wave height
(Hs). If supplied, \code{fetch_km} is ignored for Hs computation.
\item Both absent: wave exposure is estimated from depth alone (coarse proxy;
a warning is issued).
}
}
\examples{
\dontrun{
# Fetch column in result
result$fetch_km <- c(2.5, 8.0, 25.0, 45.0)
result <- score_wave_exposure(result, fetch_col = "fetch_km")

# Uniform fetch for a sheltered sea loch
result <- score_wave_exposure(result, fetch_km = 3.5)

# Measured wave height column
result <- score_wave_exposure(result, wave_height_col = "hs_m")

# Depth proxy only (coarse)
result <- score_wave_exposure(result)
}
}
